# TODO: terminal-first MBSE CLI

- Add a dedicated CLI for using `mbse` without any viewer.
- The future CLI should focus on model/runtime tasks such as validation, execution, and automation.
- It should also expose a clean API surface for tests and automated model validation flows that need the runtime. (reutilitzar la mateixa api que s'utilitza per fer un viewer o qualsevol altre cosa, si fa falta extendrela)
