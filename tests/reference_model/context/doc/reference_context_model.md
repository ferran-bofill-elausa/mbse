# Reference Context Model

This folder contains the canonical context test fixture for one representative shared context model.

- `reference_context_model.json` is the context model.

`full_reset_requested` is set by the HSM before calling the reference Activity
model and consumed by that Activity's reset decision.
