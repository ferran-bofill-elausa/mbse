# Reference Project

This folder contains the canonical multi-model project fixture.

- `reference_project.json` loads the reference Context, HSM, and Activity models.
- The entrypoint is `reference_hsm`.
