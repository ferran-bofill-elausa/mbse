# Reference HSM Model

This folder contains the canonical HSM test fixture for one representative HSM model.

- `reference_hsm_model.json` is the HSM model.
- `reference_hsm_callables.py` HSM model activities used by the tests.
